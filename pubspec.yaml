name: safari_canvaskit
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State Management
  bloc: ^8.0.3
  flutter_bloc: ^8.0.1

  # Hero Icons - also tried the latest master branch of SVG picture
  heroicons: ^0.4.0

  # Cached Network Image
  cached_network_image: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
